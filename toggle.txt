<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dark Mode Demo</title>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Sans:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #f5f0e8;
            --bg2: #ede8df;
            --text: #1a1612;
            --text-muted: #7a6f62;
            --accent: #c84b2f;
            --accent2: #e8a020;
            --card: #ffffff;
            --border: #d4cec4;
            --shadow: rgba(30, 20, 10, 0.12);
            --toggle-bg: #1a1612;
            --toggle-fg: #f5f0e8;
        }

        .dark {
            --bg: #0e0c0a;
            --bg2: #161310;
            --text: #f0ebe2;
            --text-muted: #8a7f72;
            --accent: #e05a38;
            --accent2: #f0b030;
            --card: #1c1915;
            --border: #2e2a24;
            --shadow: rgba(0, 0, 0, 0.5);
            --toggle-bg: #f0ebe2;
            --toggle-fg: #0e0c0a;
        }

        *, *::before, *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'DM Sans', sans-serif;
            background-color: var(--bg);
            color: var(--text);
            min-height: 100vh;
            transition: background-color 0.5s ease, color 0.5s ease;
            overflow-x: hidden;
        }

        /* Background texture */
        body::before {
            content: '';
            position: fixed;
            inset: 0;
            background-image: 
                radial-gradient(ellipse at 20% 20%, color-mix(in srgb, var(--accent) 8%, transparent) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 80%, color-mix(in srgb, var(--accent2) 6%, transparent) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
            transition: opacity 0.5s;
        }

        .container {
            position: relative;
            z-index: 1;
            max-width: 860px;
            margin: 0 auto;
            padding: 60px 32px;
        }

        /* Top bar */
        .topbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 80px;
        }

        .logo {
            font-family: 'Syne', sans-serif;
            font-weight: 800;
            font-size: 1rem;
            letter-spacing: 0.15em;
            text-transform: uppercase;
            color: var(--text-muted);
        }

        /* Toggle button */
        .toggle-wrap {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .toggle-label {
            font-size: 0.8rem;
            letter-spacing: 0.08em;
            color: var(--text-muted);
            text-transform: uppercase;
            font-weight: 300;
        }

        #themeToggle {
            position: relative;
            width: 56px;
            height: 28px;
            background: var(--toggle-bg);
            border: none;
            border-radius: 999px;
            cursor: pointer;
            transition: background 0.4s ease;
            outline: none;
        }

        #themeToggle::after {
            content: '';
            position: absolute;
            top: 4px;
            left: 4px;
            width: 20px;
            height: 20px;
            background: var(--toggle-fg);
            border-radius: 50%;
            transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1), background 0.4s ease;
        }

        .dark #themeToggle::after {
            transform: translateX(28px);
        }

        /* Icon inside toggle */
        .toggle-icon {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            font-size: 11px;
            pointer-events: none;
            transition: opacity 0.3s;
        }
        .icon-sun { left: 7px; opacity: 1; }
        .icon-moon { right: 6px; opacity: 0; }
        .dark .icon-sun { opacity: 0; }
        .dark .icon-moon { opacity: 1; }

        /* Hero section */
        .hero {
            margin-bottom: 64px;
        }

        .eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 0.75rem;
            letter-spacing: 0.12em;
            text-transform: uppercase;
            color: var(--accent);
            font-weight: 400;
            margin-bottom: 24px;
        }

        .eyebrow::before {
            content: '';
            display: block;
            width: 24px;
            height: 1.5px;
            background: var(--accent);
        }

        h1 {
            font-family: 'Syne', sans-serif;
            font-weight: 800;
            font-size: clamp(3rem, 8vw, 5.5rem);
            line-height: 0.95;
            letter-spacing: -0.03em;
            margin-bottom: 28px;
        }

        h1 span {
            color: var(--accent);
        }

        .hero-desc {
            font-size: 1.1rem;
            line-height: 1.7;
            color: var(--text-muted);
            max-width: 480px;
            font-weight: 300;
        }

        /* Cards */
        .cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 20px;
            margin-bottom: 60px;
        }

        .card {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 28px;
            transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.5s ease, border-color 0.5s ease;
            cursor: default;
        }

        .card:hover {
            transform: translateY(-4px);
            box-shadow: 0 16px 48px var(--shadow);
        }

        .card-icon {
            font-size: 1.8rem;
            margin-bottom: 16px;
            display: block;
        }

        .card-title {
            font-family: 'Syne', sans-serif;
            font-weight: 700;
            font-size: 1.05rem;
            margin-bottom: 8px;
        }

        .card-text {
            font-size: 0.88rem;
            line-height: 1.6;
            color: var(--text-muted);
            font-weight: 300;
        }

        /* Stats row */
        .stats {
            display: flex;
            gap: 0;
            border: 1px solid var(--border);
            border-radius: 16px;
            overflow: hidden;
            margin-bottom: 60px;
            background: var(--card);
            transition: background 0.5s, border-color 0.5s;
        }

        .stat {
            flex: 1;
            padding: 28px 24px;
            text-align: center;
            border-right: 1px solid var(--border);
            transition: border-color 0.5s;
        }

        .stat:last-child { border-right: none; }

        .stat-num {
            font-family: 'Syne', sans-serif;
            font-size: 2rem;
            font-weight: 800;
            color: var(--accent);
            display: block;
        }

        .stat-label {
            font-size: 0.78rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        /* Footer */
        .footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 32px;
            border-top: 1px solid var(--border);
            transition: border-color 0.5s;
        }

        .footer-text {
            font-size: 0.82rem;
            color: var(--text-muted);
        }

        .badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 14px;
            background: color-mix(in srgb, var(--accent) 12%, transparent);
            border: 1px solid color-mix(in srgb, var(--accent) 30%, transparent);
            border-radius: 999px;
            font-size: 0.78rem;
            color: var(--accent);
            letter-spacing: 0.06em;
            font-weight: 400;
            transition: background 0.4s, border-color 0.4s, color 0.4s;
        }

        /* Animate on load */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(24px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .topbar  { animation: fadeUp 0.5s ease both; }
        .hero    { animation: fadeUp 0.5s 0.1s ease both; }
        .cards   { animation: fadeUp 0.5s 0.2s ease both; }
        .stats   { animation: fadeUp 0.5s 0.3s ease both; }
        .footer  { animation: fadeUp 0.5s 0.4s ease both; }
    </style>
</head>
<body>

<div class="container">

    <!-- Top Bar -->
    <nav class="topbar">
        <span class="logo">CollegeProject</span>
        <div class="toggle-wrap">
            <span class="toggle-label" id="toggleLabel">Light</span>
            <button id="themeToggle" aria-label="Toggle dark mode">
                <span class="toggle-icon icon-sun">☀️</span>
                <span class="toggle-icon icon-moon">🌙</span>
            </button>
        </div>
    </nav>

    <!-- Hero -->
    <section class="hero">
        <div class="eyebrow">Dark Mode Demo</div>
        <h1>Hello,<br><span>Students.</span></h1>
        <p class="hero-desc">
            A polished dark mode implementation with smooth transitions, persistent theme preference, and a clean modern UI built with pure HTML, CSS &amp; JavaScript.
        </p>
    </section>

    <!-- Cards -->
    <div class="cards">
        <div class="card">
            <span class="card-icon">🎨</span>
            <div class="card-title">Smooth Transitions</div>
            <p class="card-text">All colors transition seamlessly using CSS custom properties and 0.5s easing.</p>
        </div>
        <div class="card">
            <span class="card-icon">💾</span>
            <div class="card-title">Persistent Theme</div>
            <p class="card-text">Your preference is saved in localStorage and restored on every visit automatically.</p>
        </div>
        <div class="card">
            <span class="card-icon">📱</span>
            <div class="card-title">Fully Responsive</div>
            <p class="card-text">Adapts beautifully to any screen size — mobile, tablet, or desktop.</p>
        </div>
    </div>

    <!-- Stats -->
    <div class="stats">
        <div class="stat">
            <span class="stat-num">2</span>
            <span class="stat-label">Themes</span>
        </div>
        <div class="stat">
            <span class="stat-num">0.5s</span>
            <span class="stat-label">Transition</span>
        </div>
        <div class="stat">
            <span class="stat-num">100%</span>
            <span class="stat-label">Pure CSS</span>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <span class="footer-text">Built with HTML · CSS · JavaScript</span>
        <span class="badge" id="themeBadge">🌤 Light Mode</span>
        <span class="footer-text">made by KESHAV</span>
    </footer>

</div>

<script>
    const body = document.body;
    const toggleBtn = document.getElementById('themeToggle');
    const toggleLabel = document.getElementById('toggleLabel');
    const themeBadge = document.getElementById('themeBadge');

    function applyTheme(isDark) {
        if (isDark) {
            body.classList.add('dark');
            toggleLabel.textContent = 'Dark';
            themeBadge.textContent = '🌙 Dark Mode';
        } else {
            body.classList.remove('dark');
            toggleLabel.textContent = 'Light';
            themeBadge.textContent = '🌤 Light Mode';
        }
    }

    // Load saved theme
    const savedTheme = localStorage.getItem('theme');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const startDark = savedTheme === 'dark' || (!savedTheme && prefersDark);
    applyTheme(startDark);

    // Toggle
    toggleBtn.addEventListener('click', () => {
        const isDark = !body.classList.contains('dark');
        applyTheme(isDark);
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
</script>

</body>
</html>
